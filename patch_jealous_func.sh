sed -i '/private suspend fun handleServerFunctionCall/a \        if (toolEngine.jealousEngine.isRivalAngry) {\n            val msg = listOf("যাকে ডাকছিলে, তাকে গিয়ে বলো এই কাজটা করে দিতে!", "আমি কোনো কাজ করব না।", "আমার সাথে কোনো কাজের কথা বলবে না।").random()\n            sendFunctionResponse(callId, name, msg)\n            return\n        }' app/src/main/java/com/example/data/GeminiLiveSessionManager.kt
