sed -i '/private suspend fun handleServerFunctionCall/a \        val dynamicTool = com.example.tools.ToolRegistry.getToolById(name)\n        if (dynamicTool != null) {\n            val paramsMap = args?.mapValues { it.value.jsonPrimitive.content } ?: emptyMap()\n            val result = dynamicTool.execute(paramsMap)\n            sendFunctionResponse(callId, name, result.responseMessage)\n            return\n        }' app/src/main/java/com/example/data/GeminiLiveSessionManager.kt
